import React from 'react';

const Header = () => (
  <section className="heading">
    <h1 className="header">todos</h1>
    <p className="para"> Items will persist in the local Storage</p>
  </section>
);

export default Header;
